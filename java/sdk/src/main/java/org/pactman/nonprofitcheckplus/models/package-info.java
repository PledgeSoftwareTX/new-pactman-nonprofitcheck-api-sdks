/**
 * Response models.
 *
 * <p>Wire field names are preserved exactly, so what you read in the Pactman API
 * reference is what you pass to {@link org.pactman.nonprofitcheckplus.models.DataObject#get(String)}
 * — no rename table to keep in sync. Typed accessors are conveniences over the
 * same store, so a field added by a future API version stays readable without a
 * deserialization failure or an SDK upgrade.
 */
package org.pactman.nonprofitcheckplus.models;
