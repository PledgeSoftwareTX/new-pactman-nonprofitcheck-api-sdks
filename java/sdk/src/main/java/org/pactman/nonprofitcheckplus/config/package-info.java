/**
 * Client configuration: defaults, validation, and credential redaction.
 *
 * <p>Everything a caller can set is here, and everything invalid is rejected
 * where the client is built rather than on the first request.
 */
package org.pactman.nonprofitcheckplus.config;
