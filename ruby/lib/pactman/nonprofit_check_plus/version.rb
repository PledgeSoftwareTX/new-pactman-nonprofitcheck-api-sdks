# frozen_string_literal: true

module Pactman
  module NonprofitCheckPlus
    # The gem version, reported in the User-Agent header.
    #
    # The gemspec reads this constant rather than restating it, so the published
    # version and the version on the wire cannot disagree.
    VERSION = "1.0.0"

    # The RubyGems name, reported in the User-Agent header.
    GEM_NAME = "pactman-nonprofit-check-plus"
  end
end
